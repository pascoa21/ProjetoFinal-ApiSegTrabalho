/*const mongoose = require('mongoose');
const products = require('../Models/products');
const Risc = mongoose.model('Product');

exports.getByRisc = (Risc) => {
    Product.find({
        Risc: req.params.Risc,
        active: true
    })}
*/

